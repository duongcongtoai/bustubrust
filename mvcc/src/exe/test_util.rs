use crate::{
    storage::table::{Column, DataTable, ValueType},
    types::Oid,
};

pub fn create_table(tuple_per_tile_group: usize, table_id: Oid) -> DataTable {
    unimplemented!()
    // let schema = Schema::new(vec![gen_col(1)]);
}
